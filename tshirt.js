var size = 20;
var text = "This is a T-shirt";
function makeShirt(size, text) {
    console.log("The size of the T-shirt is ".concat(size, ", and the text printed on it is \"").concat(text, "\""));
}
makeShirt(size, text);
