let size: number = 20
let text: string = "This is a T-shirt" 

function makeShirt(size: number, text: string){
    console.log(`The size of the T-shirt is ${size}, and the text printed on it is "${text}"`);
}

makeShirt(size, text);